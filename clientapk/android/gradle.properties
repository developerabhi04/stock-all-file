# Project-wide Gradle settings

# Memory optimization
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Parallel builds
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.configureondemand=true

# Build cache (Gradle's native cache, not Android's)
org.gradle.caching=true

# AndroidX
android.useAndroidX=true
android.enableJetifier=true

# Only build for ARM architectures
reactNativeArchitectures=armeabi-v7a,arm64-v8a

# Disable new architecture
newArchEnabled=false

# Enable Hermes engine
hermesEnabled=true

# Edge to edge
edgeToEdgeEnabled=false

# Faster builds
org.gradle.workers.max=4


MYAPP_UPLOAD_STORE_FILE=tradehub-upload.keystore
MYAPP_UPLOAD_STORE_PASSWORD=dolandtrump
MYAPP_UPLOAD_KEY_ALIAS=tradehub-upload
MYAPP_UPLOAD_KEY_PASSWORD=dolandtrump